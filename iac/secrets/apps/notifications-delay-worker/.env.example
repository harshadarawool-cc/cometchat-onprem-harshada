# notifications-delay-worker — application environment. Fill every value; this is the .env the notifications-delay-worker pod mounts.
# Also mounts (shared k8s secrets): wildcard-tls (via the nginx sidecar)
# Values REDACTED — keys show what this app requires; supply real values at deploy time.

DRY_RUN=
KAFKA_BROKERS=
KAFKA_CLIENT_ID=
KAFKA_TOPIC=
QUEUE_NAME=
REDIS_DB=
REDIS_SENTINELS=
REDIS_SENTINEL_NAME=
